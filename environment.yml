name: nodal
channels:
  - conda-forge
  - defaults
dependencies:
  - black
  - ipython
  - isort
  - obspy
  - openpyxl
  - pip
  - pygmt
  - rioxarray
  - simplekml
  - pip:
    - mat73
    - --editable ~/repos/infresnel
    - --editable ~/work/python_util
    - --editable .  # Installs packages specified in setup.py
