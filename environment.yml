name: nodal
channels:
  - conda-forge
  - defaults
dependencies:
  - black
  - ipython
  - isort
  - obspy
  - openpyxl
  - pip
  - pygmt
  - pip:
    - mat73
    - --editable .  # Installs packages specified in setup.py
