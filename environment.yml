name: nodal
channels:
  - conda-forge
  - defaults
dependencies:
  - black
  - ipython
  - isort
  - numba
  - obspy
  - openpyxl
  - pip
  - pygmt
  - rioxarray
  - simplekml
  - pip:
    - mat73
    - --editable ${HOME}/repos/infresnel
    - --editable ${HOME}/work/python_util
    - --editable .  # Installs packages specified in setup.py
